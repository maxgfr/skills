interface:
  display_name: "Verify"
  short_description: "Run evidence-backed checks on completed work"
  default_prompt: "Use $verify to check this completed work."

policy:
  allow_implicit_invocation: false
