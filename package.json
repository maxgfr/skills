{
  "name": "maxgfr-skills",
  "version": "1.4.0",
  "private": true,
  "description": "maxgfr's process skills for agent-driven engineering",
  "repository": {
    "type": "git",
    "url": "https://github.com/maxgfr/skills"
  },
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-skills.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "doctor": "node scripts/doctor.mjs",
    "test:e2e:hosts": "node scripts/e2e-hosts.mjs",
    "check": "npm run validate && npm test && npm run check-plugin-version",
    "check-plugin-version": "node scripts/sync-plugin-version.mjs --check"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^25.0.9"
  }
}
